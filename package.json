{
  "name": "kya-pehnu-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Kya Pehnu? — hyper-local fashion delivery for Nagpur. Express backend + one unified Expo app serving both the customer and vendor flows.",
  "scripts": {
    "dev": "concurrently -n backend,app -c magenta,cyan --kill-others-on-fail \"npm:dev:backend\" \"npm:dev:app\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:app": "npm --prefix customer-app run start",
    "start:backend": "npm --prefix backend start",
    "install:all": "npm install && npm --prefix backend install && npm --prefix customer-app install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20.19.4"
  }
}
